# goqueue Documentation Site

Run docs from this folder:

```bash
cd docs
npm install
npm run docs-start
```

Build for static publish:

```bash
cd docs
npm run docs-build
```

All product usage and operational documentation lives in `docs/docs`.
