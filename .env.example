# Redis connection used by apps and optional integration tests
GOQUEUE_REDIS_URL=

# Optional namespace used by SDK and inspector CLI
GOQUEUE_NAMESPACE=

# Integration tests are opt-in because they require a running Redis server
GOQUEUE_RUN_INTEGRATION_TESTS=false
